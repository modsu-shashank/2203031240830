const mongoose = require('mongoose');const urlSchema = new mongoose.Schema({  originalUrl: {    type: String,    required: true,    trim: true  },  shortCode: {    type: String,    required: true,    unique: true,    trim: true  },  createdAt: {    type: Date,    default: Date.now  },  expiresAt: {    type: Date,    required: true  },  clicks: {    type: Number,    default: 0  },  lastAccessed: {    type: Date,    default: null  }});urlSchema.index({ expiresAt: 1 }, { expireAfterSeconds: 0 });urlSchema.statics.isShortCodeAvailable = async function(shortCode) {  const count = await this.countDocuments({ shortCode });  return count === 0;};urlSchema.methods.incrementClicks = async function() {  this.clicks += 1;  this.lastAccessed = new Date();  await this.save();};const Url = mongoose.model('Url', urlSchema);module.exports = Url;